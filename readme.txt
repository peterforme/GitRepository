 /* 
		$scope.onFileSuccess = function(imageURI){
	var image = document.querySelector("#ImgUse");
    image.src = imageURI;
		};
		
		$scope.onFileFail = function(message) {
    alert('Failed because: ' + message);
		};

		$scope.onFileCameraClick = function(){
          alert("camera!");
          navigator.camera.getPicture($scope.onFileSuccess, $scope.onFileFail, { quality: 50,
   		  destinationType: Camera.DestinationType.FILE_URI
		});
          };
          */